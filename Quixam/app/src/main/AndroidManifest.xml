<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ma.enset.quixam">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quixam"
        android:usesCleartextTraffic="true">
        <activity android:name=".add_edit_question"></activity>
        <activity android:name=".add_edit_questions" />
        <activity android:name=".add_edit_quiz" />
        <activity android:name=".list_quizes_teacher" />
        <activity android:name=".signIn" />
        <activity android:name=".login" />
        <activity android:name=".choose_userType" />
        <activity android:name=".landing_page" />
        <activity android:name=".Quiz_score" />
        <activity android:name=".taking_quiz" />
        <activity android:name=".before_start_quiz" />
        <activity android:name=".Student_quizes" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>